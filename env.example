# ArXiv MCP Server Configuration
# Copy this file to .env and configure the variables below

# Port configuration
# The external port where the MCPO wrapper will be exposed
HOST_PORT=18001

# Temporary storage configuration
# Path for temporary paper processing during analysis
# Papers are loaded into LLM context, not persisted
DOWNLOAD_PATH=/tmp/arxiv_temp

# OpenWebUI Integration
# Use this URL in OpenWebUI Tools configuration
# Replace YOUR_HOST with your actual server IP or hostname
OPENWEBUI_TOOL_URL=http://YOUR_HOST:18001

# Optional: Logging configuration
LOG_LEVEL=INFO

# Optional: arXiv API configuration
# These are optional - the server uses arXiv defaults if not specified
# ARXIV_MAX_RESULTS=10
# ARXIV_SORT_BY=relevance
# ARXIV_SORT_ORDER=descending

# Network configuration (advanced)
# DOCKER_NETWORK=arxiv-network
